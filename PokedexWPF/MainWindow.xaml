<Window x:Class="PokedexWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PokedexWPF"
        mc:Ignorable="d"
        Title="PokédexPro" Height="720" Width="1280" Background="#92d1b4" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <SolidColorBrush x:Key="BackgroundColor" Color="#0F172A"/>
        <SolidColorBrush x:Key="ButtonBackground" Color="#f8f9fa" Opacity=".9"/>

        <Style x:Key="Titel" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
            <Setter Property="FontFamily" Value="/PokedexWPF;component/Resources/Fonts/Nunito/#Nunito"/>
            <Setter Property="FontSize" Value="36"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#ffffff"/>
        </Style>

        <Style x:Key="Caption" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
            <Setter Property="FontFamily" Value="/PokedexWPF;component/Resources/Fonts/Nunito/#Nunito"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#ffffff"/>
        </Style>

        <Style x:Key="Text" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
            <Setter Property="FontFamily" Value="/PokedexWPF;component/Resources/Fonts/Nunito/#Nunito"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#ffffff"/>
        </Style>

        <Style x:Key="Button" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="FontFamily" Value="/PokedexWPF;component/Resources/Fonts/Nunito/#Nunito"/>
            <Setter Property="FontSize" Value="25"/>
            <Setter Property="Background" Value="{StaticResource ButtonBackground}"/>
            <Setter Property="Foreground" Value="#212529"/>
            <Setter Property="BorderBrush" Value="{StaticResource ButtonBackground}"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"/>
                </Style>
            </Style.Resources>
        </Style>

        <Style x:Key="InputBox" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="FontFamily" Value="/PokedexWPF;component/Resources/Fonts/Nunito/#Nunito"/>
            <Setter Property="FontSize" Value="25"/>
            <Setter Property="Foreground" Value="#212529"/>
            <Setter Property="Background" Value="{StaticResource ButtonBackground}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="{StaticResource ButtonBackground}"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"/>
                </Style>
            </Style.Resources>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.2*"/>
            <RowDefinition Height="0.8*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <StackPanel DockPanel.Dock="Right" VerticalAlignment="Center" Margin="20,0,0,0">
                    <TextBlock Text="#001" Style="{StaticResource Caption}">
                        <TextBlock.Effect>
                            <DropShadowEffect ShadowDepth="2" Color="Black" Opacity="0.25" BlurRadius="5"/>
                        </TextBlock.Effect>
                    </TextBlock>

                    <TextBlock Text="Bulbasaur" Style="{StaticResource Titel}" Margin="0,2,0,0">
                        <TextBlock.Effect>
                            <DropShadowEffect ShadowDepth="2" Color="Black" Opacity="0.25" BlurRadius="5"/>
                        </TextBlock.Effect>
                    </TextBlock>
                </StackPanel>
            </Grid>

            <Grid Grid.Column="1">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" DockPanel.Dock="Left">

                    <TextBox Width="300" Height="50" Style="{StaticResource InputBox}" TextWrapping="Wrap" Text="Search Name/Number" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
                    <Button Content="Go!" Width="100" Style="{StaticResource Button}" Height="50" Margin="20,0,0,0"></Button>

                </StackPanel>
            </Grid>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.03*"/>
                <ColumnDefinition Width="0.12*"/>
                <ColumnDefinition Width="0.4*"/>
                <ColumnDefinition Width="0.4*"/>
                <ColumnDefinition Width="0.05*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">


            </Grid>

            <Grid Grid.Column="1">
                <StackPanel DockPanel.Dock="Right" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,0">
                    <TextBlock Text="Height: 0,7m" Style="{StaticResource Text}">
                        <TextBlock.Effect>
                            <DropShadowEffect ShadowDepth="2" Color="Black" Opacity="0.25" BlurRadius="5"/>
                        </TextBlock.Effect>
                    </TextBlock>

                    <TextBlock Text="Weigth: 6,9kg" Style="{StaticResource Text}">
                        <TextBlock.Effect>
                            <DropShadowEffect ShadowDepth="2" Color="Black" Opacity="0.25" BlurRadius="5"/>
                        </TextBlock.Effect>
                    </TextBlock>

                </StackPanel>

            </Grid>

            <Grid Grid.Column="2" Margin="88,96,88,96">
                <Image Source="/PokedexWPF;component/Resources/Images/1.png" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>


            <Grid Grid.Column="3">
                <StackPanel DockPanel.Dock="Right" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,0">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" DockPanel.Dock="Left">
                        <Image Source="/PokedexWPF;component/Resources/Images/grass.png" Width="150"/>
                        <Image Source="/PokedexWPF;component/Resources/Images/poison.png" Width="150" Margin="20,0,0,0"/>
                    </StackPanel>
                    
                    <TextBlock Text="Base stats:" Style="{StaticResource Titel}">
                        <TextBlock.Effect>
                            <DropShadowEffect ShadowDepth="2" Color="Black" Opacity="0.25" BlurRadius="5"/>
                        </TextBlock.Effect>
                    </TextBlock>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" DockPanel.Dock="Left">
                        <TextBlock Text="HP: 45" Style="{StaticResource Caption}"/>
                        <TextBlock Text="Attack: 49" Style="{StaticResource Caption}" Margin="20,0,0,0"/>
                        <TextBlock Text="Defense: 49" Style="{StaticResource Caption}" Margin="20,0,0,0"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" DockPanel.Dock="Left">
                        <TextBlock Text="Sp. Attack: 65" Style="{StaticResource Caption}"/>
                        <TextBlock Text="Sp. Defense: 65" Style="{StaticResource Caption}" Margin="20,0,0,0"/>
                        <TextBlock Text="Speed: 45" Style="{StaticResource Caption}" Margin="20,0,0,0"/>
                    </StackPanel>


                </StackPanel>

            </Grid>
        </Grid>

    </Grid>
</Window>
